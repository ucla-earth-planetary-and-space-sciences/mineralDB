---
- name: Creating default user
  become: true
  user: name={{ prname }} password=pass shell=/bin/bash home=/home/{{prname}}

- name: Copying ssh keys
  become: true
  copy: src=~/.ssh dest=/home/{{ prname }}/ owner={{ prname }} mode=600
